# mubuagent

`mubuagent` 是一个基于 FastAPI 的智能聊天机器人服务，支持普通 LLM 对话，并能让大模型根据用户问题自动判断是否需要调用本地知识库检索工具。

项目适合用于个人或企业的本地知识库助手、项目文档问答、智能客服原型，以及 RAG 与 Tool Calling 结合的应用实验。

## 功能特性

- **普通 LLM 对话**：支持通用聊天、问答、总结、解释等常规大模型能力。
- **知识库工具调用**：当问题涉及本地文档或项目资料时，由大模型自动调用知识库检索工具。
- **本地知识库检索**：支持对本地文档构建索引，并通过混合检索获取相关内容。
- **多轮会话能力**：支持会话历史保存，并在上下文过长时进行压缩。
- **Web 聊天界面**：内置简洁的网页端聊天入口，可直接在浏览器中使用。
- **索引重建接口**：支持通过页面按钮或 API 重新构建知识库索引。

## 工作方式

系统收到用户消息后，会先交给聊天机器人模块处理。大模型会根据问题内容判断是否需要查询本地知识库：

- 如果是通用问题，则直接由 LLM 生成回答。
- 如果是知识库、项目文档或本地资料相关问题，则自动调用本地知识库检索工具，并返回相关文档内容。

简化流程如下：

```text
用户提问
  ↓
FastAPI 聊天接口
  ↓
LLM 判断是否需要工具
  ├─ 普通问题：直接回答
  └─ 知识库问题：调用本地知识库检索工具
  ↓
返回最终回复
```

## 项目结构

```text
mubuagent/
├── app.py                         # FastAPI 服务入口
├── config.py                      # 项目配置与环境变量管理
├── core_rag.py                    # 本地知识库检索与索引构建
├── chatbot_agent/                 # 聊天机器人核心模块
│   ├── chatbot_agent.py           # ChatBot 主逻辑
│   ├── llm_client.py              # 大模型客户端封装
│   ├── message_builder.py         # 消息与 Prompt 构建
│   ├── session_manager.py         # 会话历史管理
│   ├── tool_decorator.py          # 工具注册
│   └── tool_executor.py           # 工具执行
├── utils/                         # 工具函数与模型封装
├── data/                          # 索引、缓存和运行数据
└── 知识库/                         # 默认本地知识库目录
```

## API 接口

### 健康检查

```http
GET /api/health
```

返回服务状态。

### 聊天接口

```http
POST /api/chat
```

请求示例：

```json
{
  "session_id": "web_chat",
  "message": "项目里的知识库工具怎么用？"
}
```

响应示例：

```json
{
  "answer": "...",
  "source": "knowledge_base",
  "contexts": [],
  "tool_results": []
}
```

其中 `source` 表示回答来源：

- `llm`：由大模型直接回答
- `knowledge_base`：调用了本地知识库工具

### 重建知识库索引

```http
POST /api/rebuild
```

用于重新扫描知识库目录并构建索引。

响应示例：

```json
{
  "document_count": 128,
  "message": "索引重建成功"
}
```

## 配置说明

项目通过 `.env` 文件和 `config.py` 管理配置。使用前需要配置大模型接口信息，例如 API Key、接口地址和模型名称；如果需要更换知识库目录，也可以通过环境变量指定本地文档路径。

知识库检索、文档切片和召回数量等参数已经在项目中提供默认值，一般情况下无需修改。首次运行时，只需要准备好大模型 API Key，并将需要检索的文档放入默认知识库目录即可。

> 请不要将真实 `.env` 文件或 API Key 提交到 GitHub。

## 快速开始

### 1. 安装依赖

根据项目依赖安装 Python 运行环境。核心依赖包括：

- `fastapi`
- `uvicorn`
- `pydantic`
- `python-dotenv`
- `chromadb`
- `rank_bm25`
- `numpy`
- `requests`
- `loguru`

### 2. 配置环境变量

在项目根目录创建 `.env` 文件，并配置大模型接口信息。

### 3. 准备知识库文档

将需要检索的文档放入 `知识库/` 目录，或通过 `DOC_DIR` 指定其他目录。

### 4. 启动服务

```bash
uvicorn app:app --reload
```

浏览器访问：

```text
http://127.0.0.1:8000
```

### 5. 构建知识库索引

首次使用或文档更新后，需要重建索引：

- 在 Web 页面点击重建索引按钮
- 或调用 `POST /api/rebuild`

## 适用场景

- 项目文档智能助手
- 企业内部知识库问答
- 私有资料检索机器人
- 智能客服原型
- RAG 与 Tool Calling 实验
- 多轮对话助手应用

## 后续扩展方向

- 支持流式输出
- 支持多知识库切换
- 增加登录鉴权与权限控制
- 增加文档上传与管理页面
- 增加检索来源可视化引用
- 将工具检索结果交给 LLM 进一步总结生成

## 项目简介

`mubuagent` 是一个基于 FastAPI 的智能聊天机器人服务，既可以进行普通大模型对话，也可以在需要时自动调用本地知识库检索工具，实现通用聊天与本地资料问答的灵活结合。
